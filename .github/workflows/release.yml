name: Auto Build & Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Extract Version Info
        id: get_version
        run: |
          V_CODE=$(grep -oE 'versionCode\s*=?\s*[0-9]+' frontend/android/app/build.gradle* frontend/android/app/version.properties 2>/dev/null | head -1 | grep -oE '[0-9]+')
          V_NAME=$(grep -oE 'versionName\s*=?\s*["'\''"][^"'\'']+["'\''"]' frontend/android/app/build.gradle* frontend/android/app/version.properties 2>/dev/null | head -1 | sed -E 's/.*["'\''"]([^"'\'']+)["'\''"]/\1/')
          
          if [ -z "$V_CODE" ]; then V_CODE="23"; fi
          if [ -z "$V_NAME" ]; then V_NAME="1.0.23"; fi
          
          echo "version_code=$V_CODE" >> $GITHUB_OUTPUT
          echo "version_name=$V_NAME" >> $GITHUB_OUTPUT
          echo "tag=v$V_NAME" >> $GITHUB_OUTPUT

      - name: Check if Tag Exists
        id: check_tag
        run: |
          TAG="${{ steps.get_version.outputs.tag }}"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "Tag $TAG already exists. Skipping build."
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Build Debug APK
        if: steps.check_tag.outputs.exists == 'false'
        working-directory: frontend/android
        run: ./gradlew assembleDebug

      - name: Create Release & Upload APK
        if: steps.check_tag.outputs.exists == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.get_version.outputs.tag }}
          name: Release ${{ steps.get_version.outputs.tag }} (Build ${{ steps.get_version.outputs.version_code }})
          files: frontend/android/app/build/outputs/apk/debug/app-debug.apk
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
